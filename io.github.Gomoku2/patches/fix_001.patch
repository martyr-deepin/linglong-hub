--- ../Gomoku.pro	2023-12-19 20:54:35.081959000 +0800
+++ ../Gomoku.pro	2023-12-19 20:50:20.882475000 +0800
@@ -10,7 +10,21 @@
 
 TARGET = Gomoku
 TEMPLATE = app
+target.path = $$PREFIX/bin # /opt/$${TARGET}/bin
+INSTALLS += target
 
+DESKTOP_FILE = ./Gomoku.desktop
+system("echo '[Desktop Entry]' > $$DESKTOP_FILE")
+system("echo 'Type=Application' >> $$DESKTOP_FILE")
+system("echo 'Name=Gomoku' >> $$DESKTOP_FILE")
+system("echo 'Comment=Gomoku.' >> $$DESKTOP_FILE")
+system("echo 'Exec=Gomoku' >> $$DESKTOP_FILE")
+system("echo 'Terminal=false' >> $$DESKTOP_FILE")
+system("echo 'Categories=Utility;' >> $$DESKTOP_FILE")
+
+desktop.files += $$DESKTOP_FILE
+desktop.path = $$PREFIX/share/applications
+INSTALLS += desktop
 # The following define makes your compiler emit warnings if you use
 # any feature of Qt which as been marked as deprecated (the exact warnings
 # depend on your compiler). Please consult the documentation of the
