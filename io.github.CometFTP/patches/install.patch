diff --git a/CometFTP.desktop b/CometFTP.desktop
new file mode 100644
index 0000000..d1e3616
--- /dev/null
+++ b/CometFTP.desktop
@@ -0,0 +1,8 @@
+[Desktop Entry]
+Categories=Game;Qt;
+Exec=CometFTP
+Name=CometFTP
+StartupNotify=false
+Terminal=false
+Type=Application
+X-Deepin-Vendor=user-custom
diff --git a/CometFTP.pro b/CometFTP.pro
index a8d4df4..4929458 100644
--- a/CometFTP.pro
+++ b/CometFTP.pro
@@ -48,3 +48,10 @@ CONFIG += c++11
 #win32: LIBS += -lssh
 #win32: LIBS += -llibeay32
 #win32: LIBS += -lssleay32
+
+BINDIR = $$PREFIX/bin
+DATADIR = $$PREFIX/share
+target.path = $$BINDIR
+desktop.files =CometFTP.desktop
+desktop.path = $$DATADIR/applications/
+INSTALLS += target desktop

